FROM python:3.11-slim

WORKDIR /app

COPY *.py .env* /app/

RUN pip install --no-cache-dir fastapi uvicorn python-dotenv ibm-watsonx-ai pydantic

EXPOSE 8000

CMD ["uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8000"]
